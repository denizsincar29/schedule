# Введение
В Северном Арктическом Федеральном Университете (САФУ) доступ к информационным ресурсам осуществляется через две платформы: SAKAI и Modeus. Через последнюю студенты и преподаватели могут просматривать расписание занятий, оценки, посещаемость и другую информацию. К Modeus подключён не только САФУ, но и ТюмГУ и возможно другие ВУЗы.
В рамках данного исследования была разработана программа для просмотра расписания занятий в Северном (Арктическом) федеральном университете.
# 1 Определение требований к разрабатываемому приложению (краткое техническое задание) 
Программа разрабатывается на Python3 с использованием библиотек WXPython и HTTPX.
Программа в первую очередь ориентирована на незрячих пользователей, поэтому для графического интерфейса используется библиотека [WXPython](https://wxpython.org/). Программы, написанные на этой библиотеке, имеют нативный для платформы интерфейс, поэтому полностью доступны для программ чтения с экрана.
## 1.1 Назначение и цели создания приложения
Программа предназначена для просмотра расписания занятий в Северном (Арктическом) федеральном университете. Целью создания программы является упрощение доступа к информации о расписании занятий для всех пользователей.
## 1.2 Требования к программе
- ( ) Программа должна быть кроссплатформенной и работать на Windows, Linux и MacOS. (частично выполнено)
- (x) Программа должна быть доступна для программ чтения с экрана. (На MacOS и Linux пока не озвучиваются подсказки. Чтобы их услышать, нужно нажать горячую клавишу для озвучивания статусной строки.).
- (x) Программа должна получать данные с сервера Modeus и корректно их парсить и отображать.
    Примечание: JSON ответ сервера очень сложный и не всегда корректно парсится. Например, в расписании встречаются занятия, которые ведут несколько преподавателей, и привычная строка teacher превращается в список имён, что приводило старую версию программы к вылету.
- (x) Программа должна кешировать расписание на месяц, чтобы не делать запросы к серверу каждый раз.
## 1.3 Состав и содержания работ по созданию программы
- (x) Разработка консольного клиента (для тестирования функционала модуля).
- (x) Разработка графического интерфейса, который будет как доступен для программ чтения с экрана, так и удобен для обычных пользователей.
- (x) Разработка модуля для работы с сервером Modeus.
- (x) Разработка модуля верхнего уровня для получения, кеширования расписания, авторизации, поиска и кеширования студентов и преподавателей...
## 1.4 Порядок контроля и приемки приложения
Выполненная курсовая работа сдается в несколько этапов в установленные техническим заданием сроки. На первом этапе преподавателем производиться оценка программного кода на соответствие стандарту оформления PEP8 и PEP257, а также  работоспособность кода. На втором этапе производится оценка текста пояснительной записки к курсовому проекту на соответствие стандарту СТО 60-02.2.3-2018. На третьем этапе производиться защита курсовой работе в формате выступления с презентацией

## 1.5 Требования к документированию
- (x) Документация к программе должна быть написана ввиде Docstrings ко всем классам и функциям по стандартам PEP257 и СТО 60-02.2.3-2018.

# 2 Проектирование приложения
## 2.1 Проектирование вариантов использования приложения
В режиме работы при подключении к интернету программа должна получить актуальное расписание на месяц и его кешировать. В онлайн режиме так же доступен просмотр чужого расписания, просмотр пересечения расписаний и авторизация.
В оффлайн режиме доступен просмотр кешированного расписания, При этом только своего. Просмотр чужого расписания и авторизация недоступны.
## 2.2 Описание выделенных классов
- schedule.Schedule - главный класс, который отвечает за получение, кеширование и расписания, поиск студентов и преподавателей, авторизацию...
- parsers.people.Person - класс, который отвечает за хранение информации о студенте или преподавателе. Список людей хранится в parsers.people.People.
- parsers.events.Event - класс, который отвечает за хранение информации о занятии. Список занятий хранится в parsers.events.Events.
    классы People и Events- это контейнеры для хранения людей/занятий. Они предоставляют методы для фильтрации, вывода, поиска, хранения и других операций над списками людей/занятий.
- parsers.people.(Student | Employee) - классы, которые наследуются от Person и отвечают за хранение информации о студенте и преподавателе соответственно.
- parsers.people.NoOne - класс, который наследуется от Person и используется для замены несуществующего студента или преподавателя.
- parsers.mess- модуль-помощник для работы с данными с сервера.
- guinput - модуль, который отвечает за ввод данных от пользователя. В нём есть классы для ввода логина и пароля, текста, выбора из списка, показа сообщения и показа QR-кода.
ical_share - модуль, который отвечает за загрузку и сохранение календаря в формате iCal на сервере, чтобы перевести его в ссылку для загрузки. В нём есть несколько функций для загрузки календаря и получения ссылки. Так же есть класс GotICalCheck(threading.Thread), который проверяет, получил ли пользователь календарь и сигнализирует об этом, чтобы закрыть окно с QR-кодом после сканирования.
- app_logic.App - класс, который отвечает за логику работы программы. Наследуется от threading.Thread и работает с классом schedule.Schedule.
main.pyw - главный файл программы. Запускает графический интерфейс и создаёт объект класса app_logic.App.
# 3 Реализация приложения
## 3.1 Выбор языка программирования и среды разработки
Программа разрабатывается на Python3. Выбор языка обусловлен тем, что Python3 является одним из самых популярных языков программирования и имеет множество библиотек для работы с сетью, графическим интерфейсом и для других задач.
- достоинства
    - Простой и понятный синтаксис.
    - множество библиотек, разработанных сообществом.
    - Поддержка ООП.
    - Кроссплатформенность.
- недостатки
    - Низкая скорость выполнения.
    - динамическая типизация.

Программа разрабатывается в Visual Studio Code, так как это единственная IDE, которая доступна для незрячих.
- достоинства
    - Бесплатная.
    - Поддержка множества языков программирования.
    - Поддержка плагинов.
    - Кроссплатформенность.
    - поддержка github copilot.

## 3.2 Выбор и обоснование используемых библиотек
- [WXPython](https://wxpython.org/) - библиотека для создания графического интерфейса. Выбрана из-за того, что она имеет нативный для платформы интерфейс и полностью доступна для программ чтения с экрана для незрячих пользователей.
- [HTTPX](https://www.python-httpx.org/) - библиотека для работы с сетью. Выбрана из-за того, что автор программы не смог заставить программу получать данные с сервера Modeus с помощью библиотеки requests или AIOHTTP.
- [icalendar](https://icalendar.readthedocs.io/en/latest/) - библиотека для работы с календарями в формате iCal. Выбор обусловлен тем, что она позволяет легко создавать и сохранять календари в формате iCal.
- [qrcode](https://pypi.org/project/qrcode/) - библиотека для создания QR-кодов. Самая популярная библиотека для создания QR-кодов на Python. Для обработки изображений используется библиотека [Pillow](https://python-pillow.org/).
- [python-dateutil
](https://dateutil.readthedocs.io/en/stable/) - библиотека для работы с датами. Выбрана из-за того, что она позволяет легко работать с датами и временем.
- [pytz](https://pytz.sourceforge.io/) - библиотека для работы с часовыми поясами. Необходима для работы с датами и временем в московском часовом поясе.
- [pydotdict](https://pypi.org/project/pydotdict/) - библиотека для превращения словарей в своеобразные объекты, у которых можно обращаться к ключам как к атрибутам. Необходима для удобной работы с данными с сервера Modeus. Выбрана экспериментально.
- Для тестирования используется библиотека [pytest](https://docs.pytest.org/en/6.2.x/). Выбрана из-за того, что она позволяет легко писать тесты и имеет множество плагинов для тестирования.
- для компиляции программы в exe используется библиотека [pyinstaller](https://www.pyinstaller.org/). Самая популярная библиотека для компиляции программ на Python в exe.
- для создания инсталлятора используется библиотека [Inno Setup](https://www.jrsoftware.org/isinfo.php). С помощью неё можно создать самые доступные для незрячих инсталляторы.

## 3.3 Описание разработанных алгоритмов
Для получения данных через API сервера Modeus сперва программа передаёт логин и пароль и получает токен авторизации, который живёт 12 часов. С помощью этого токена осуществляется вся дальнейшая работа с сервером. При каждом запросе программа проверяет, не истёк ли токен, и если истёк, то получает новый. Токен хранится в json файле, чтобы не получать его при перезапуске программы, так как операция получения токена занимает много времени и делает несколько запросов к серверу.


## 3.4 Описание реализованных классов, их свойств и методов
## 3.4 Описание реализованных классов, их свойств и методов

### Класс `schedule.schedule.Config`

Класс для чтения и записи конфигурационного файла. Наследуется от `DotDict`, что позволяет обращаться к ключам конфигурации как к атрибутам объекта.

#### Методы:

- `__init__()`: Инициализирует объект `Config`, читая конфигурационный файл или создавая новый с значениями по умолчанию.
- `read_config()`: Читает конфигурационный файл и создает объект `dotdict`.
- `save_config()`: Сохраняет объект `dotdict` в конфигурационный файл.

### Класс `schedule.schedule.Schedule`

Класс для взаимодействия с API Modeus и управления расписанием.

#### Свойства:

- `email: str`: Электронная почта пользователя.
- `password: str`: Пароль пользователя.
- `token: str`: Токен для доступа к API.
- `expire: datetime`: Время истечения токена.
- `no_internet: bool`: Флаг отсутствия интернета.
- `results: schedule.parsers.People`: Результаты поиска людей.
- `people: schedule.parsers.People`: Список людей.
- `last_events: schedule.parsers.Events`: Последние события.
- `last_msg: str`: Последнее сообщение.
- asyncio.Event`schedstop`: Событие для остановки потока расписания.
- `config: Config`: Объект конфигурации.

#### Методы:

- `__init__(email: str, password: str, on_error=lambda: None)`: Инициализирует объект `Schedule` с использованием email и пароля.
  - Аргументы:
    - `email` (str): Электронная почта пользователя.
    - `password` (str): Пароль пользователя.
    - `on_error` (function): Функция для вызова при ошибке. По умолчанию - пустая функция.
- `save_token()`: Сохраняет токен и время его истечения в конфигурационный файл.
- `load_token()`: Загружает токен и время его истечения из конфигурационного файла.
- `check_token(on_error=lambda: None)`: Проверяет, истек ли токен или не загружен, и получает новый токен, если необходимо.
  - Аргументы:
    - `on_error` (function): Функция для вызова при ошибке. По умолчанию - пустая функция.
- `save_current_person(person: Person=noone, itsme: bool=True)`: Сохраняет информацию о человеке в `people.json`.
  - Аргументы:
    - `person` (Person): Человек для сохранения.
    - `itsme` (bool): Флаг, указывающий на текущего пользователя.
- `load_people()`: Загружает информацию о людях из `people.json`.
- `get_schedule(person: Person=noone, start_time: date=None, end_time: date=None, overlap: Person=noone, on_finish=None) -> Events`: Получает расписание для заданного человека.
  - Аргументы:
    - `person` (Person): Человек для получения расписания.
    - `start_time` (date): Дата начала расписания.
    - `end_time` (date): Дата окончания расписания.
    - `overlap` (Person): Человек для получения пересекающихся событий.
    - `on_finish` (function): Функция для вызова после завершения.
  - Возвращаемое значение:
    - `Events`: Список событий.
- `get_month(month: int, person: Person=noone) -> Events`: Получает и кэширует расписание для заданного человека на месяц.
  - Аргументы:
    - `month` (int): Месяц для получения расписания.
    - `person` (Person): Человек для получения расписания.
  - Возвращаемое значение:
    - `Events`: Список событий за месяц.
- `search_in_cache(person: Person, start_date: date, end_date: date) -> Events`: Ищет расписание для заданного человека в кэше.
  - Аргументы:
    - `person` (Person): Человек для поиска.
    - `start_date` (date): Дата начала поиска.
    - `end_date` (date): Дата окончания поиска.
  - Возвращаемое значение:
    - `Events`: Список событий в указанном диапазоне.
- `search_person_from_cache(term: str, by_id: bool) -> People`: Ищет человека в кэше по имени или ID.
  - Аргументы:
    - `term` (str): Запрос для поиска (имя или ID).
    - `by_id` (bool): Флаг поиска по ID.
  - Возвращаемое значение:
    - `People`: Список людей, соответствующих запросу.
- `search_person(term: str, by_id: bool) -> People`: Ищет человека в Modeus по имени или ID.
  - Аргументы:
    - `term` (str): Запрос для поиска (имя или ID).
    - `by_id` (bool): Флаг поиска по ID.
  - Возвращаемое значение:
    - `People`: Список людей, соответствующих термину.
- `save_result(idx: int, itsme: bool=True)`: Сохраняет результат поиска в кэш.
  - Аргументы:
    - `idx` (int): Индекс результата для сохранения.
    - `itsme` (bool): Флаг, указывающий на текущего пользователя.
  - Возвращаемое значение:
    - `bool`: Истина, если сохранено, иначе ложь.
- `who_goes(event: Event) -> People`: Получает список людей, посещающих событие.
  - Аргументы:
    - `event` (Event): Событие для получения списка людей.
  - Возвращаемое значение:
    - `People`: Список людей, посещающих событие.
- `schedule(person: Person=noone, start_time: date=None, end_time: date=None, overlap: Person=noone) -> Events`: Получает расписание для заданного человека и кэширует его.
  - Аргументы:
    - `person` (Person): Человек для получения расписания.
    - `start_time` (date): Дата начала расписания.
    - `end_time` (date): Дата окончания расписания.
    - `overlap` (Person): Человек для получения пересекающихся событий.
  - Возвращаемое значение:
    - `Events`: Список событий.
- `schedule_async(person: Person=noone, start_time: date=None, end_time: date=None, overlap: Person=noone, on_finish=None)`: Получает расписание для заданного человека асинхронно и кэширует его.
  - Аргументы:
    - `person` (Person): Человек для получения расписания.
    - `start_time` (date): Дата начала расписания.
    - `end_time` (date): Дата окончания расписания.
    - `overlap` (Person): Человек для получения пересекающихся событий.
    - `on_finish` (function): Функция для вызова после завершения.

### Функция `auth`

Функция для аутентификации пользователя в системе Modeus с использованием email и пароля.

#### Аргументы:

- `email` (str): Электронная почта пользователя.
- `password` (str): Пароль пользователя.

#### Возвращаемое значение:

- `(bool, str)`: Кортеж, где первый элемент - успех (True/False), второй - сообщение об ошибке (если есть).

### Класс `schedule.parsers.people.Person`
Представляет человека.

#### Атрибуты
- `person_id` (str): Уникальный идентификатор человека.
- `name` (str): Имя человека.
- `start_date` (date | None): Дата начала.
- `end_date` (date | None): Дата окончания.

#### Методы

##### `mutate(self, other: Person) -> None`
Мутирует текущий объект с другим. Не используется почти нигде.
  - Аргументы:
    - `other` (Person): Другой объект.

##### `__eq__(self, other: Person) -> bool`
Проверяет равенство с другим объектом.

##### `__ne__(self, other: Person) -> bool`
Проверяет неравенство с другим объектом.

##### `__hash__(self) -> int`
Возвращает хэш объекта.

##### `@classmethod _from_prepared(cls, data: dict) -> Self`
Создает объект из подготовленных json данных, например из кэша.
  - Аргументы:
    - `data` (dict): Подготовленные данные.
  - Возвращаемое значение:
    - `Person`: Созданный объект.

### Класс schedule.parsers.people.Student
Представляет студента. Наследуется от класса `Person`.

#### Атрибуты
- `specialty` (str): Специальность студента.
- `profile` (str): Профиль студента.

#### Методы

##### `__post_init__(self) -> None`
Инициализирует пустые значения для `specialty` и `profile`.

##### `@property type(self) -> type`
Возвращает тип объекта.

##### `pprint(self) -> None`
Выводит информацию о студенте в удобочитаемом формате.

##### `__str__(self) -> str`
Возвращает строковое представление студента.

##### `__repr__(self) -> str`
Возвращает JSON-представление студента.

##### `__dict__(self) -> dict`
Возвращает словарь атрибутов студента.

##### `json(self) -> str`
Возвращает JSON-строку студента.

##### `@classmethod _from_prepared(cls, data: dict) -> Self`
Создает объект студента из подготовленных данных.

### Класс schedule.parsers.people.Employee
Представляет сотрудника. Наследуется от класса `Person`.

#### Атрибуты
- `group` (str): Группа сотрудника.

#### Методы

##### `__post_init__(self) -> None`
Инициализирует пустое значение для `group`.

##### `@property type(self) -> type`
Возвращает тип объекта.

##### `pprint(self) -> None`
Выводит информацию о сотруднике в удобочитаемом формате.

##### `__str__(self) -> str`
Возвращает строковое представление сотрудника.

##### `__repr__(self) -> str`
Возвращает JSON-представление сотрудника.

##### `__dict__(self) -> dict`
Возвращает словарь атрибутов сотрудника.

##### `json(self) -> str`
Возвращает JSON-строку сотрудника.

##### `@classmethod _from_prepared(cls, data: dict) -> Self`
Создает объект сотрудника из подготовленных данных.

##### `__iter__(self) -> iter`
Возвращает итератор по списку людей.

##### `__len__(self) -> int`
Возвращает количество людей в списке.

##### `__getitem__(self, index: int) -> Person`
Возвращает человека по индексу.

##### `__setitem__(self, index: int, value: Person) -> None`
Устанавливает человека по индексу.

##### `__delitem__(self, index: int) -> None`
Удаляет человека по индексу.

##### `__str__(self) -> str`
Возвращает строковое представление списка людей.

##### `__repr__(self) -> str`
Возвращает строку для отладки списка людей.

##### `__contains__(self, item: Union[str, Person]) -> bool`
Проверяет наличие человека в списке по имени или объекту.

##### `__eq__(self, other: People) -> bool`
Проверяет равенство с другой коллекцией людей.

##### `__ne__(self, other: People) -> bool`
Проверяет неравенство с другой коллекцией людей.

##### `__add__(self, other: People) -> People`
Добавляет людей, исключая дубликаты.

##### `__sub__(self, other: People) -> People`
Удаляет людей, присутствующих в другой коллекции.

##### `__dict__(self) -> dict`
Возвращает словарь атрибутов коллекции людей.

##### `pprint(self) -> None`
Выводит список людей в удобочитаемом формате.

##### `@property current(self) -> Person`
Возвращает текущего человека по ID.

##### `@current.setter current(self, pid: Union[str, Person]) -> None`
Устанавливает текущего человека по ID.

##### `@property friend(self) -> Person`
Возвращает друга по ID.

##### `@friend.setter friend(self, pid: Union[str, Person]) -> None`
Устанавливает друга по ID.

##### `append(self, person: Person) -> None`
Добавляет человека в список.

##### `json(self) -> str`
Возвращает JSON-строку списка людей.

##### `@classmethod from_who_goes(cls, data: list) -> Self`
Парсит данные о посещаемости события.

##### `@classmethod from_big_mess(cls, data: dict) -> Self`
Парсит данные с сервера о людях.

##### `@classmethod from_prepared_json(cls, data: dict) -> Self`
Парсит подготовленные JSON-данные о людях.

##### `@classmethod from_cache(cls) -> Self`
Загружает людей из кеша.

##### `to_cache(self) -> None`
Сохраняет людей в кеш.

##### `get_person_by_id(self, person_id: str) -> Person`
Возвращает человека по ID.

##### `get_people_by_name(self, name: str) -> People`
Возвращает людей по имени.

##### `get_students(self) -> People`
Возвращает студентов.

##### `get_employees(self) -> People`
Возвращает сотрудников.

##### `get_students_by_specialty(self, specialty: str) -> People`
Возвращает студентов по специальности.

##### `get_students_by_profile(self, profile: str) -> People`
Возвращает студентов по профилю.

##### `get_employees_by_group(self, group: str) -> People`
Возвращает сотру

### Класс schedule.parsers.events.Events

Представляет собой коллекцию событий. Поддерживает все методы списка и некоторые дополнительные методы для фильтрации и сравнения.

#### Атрибуты
- `events` (list[Event]): Список событий.
- `tokens` (list[tuple[Event, int]]): Для токенизации событий.
- `nocache` (bool): True, если кэш пуст, False, если есть кэш, но нет событий в указанном диапазоне фильтрации.

#### Методы

##### `__init__(self, events: list[Event]=[], nocache: bool=False) -> None`
Инициализирует коллекцию событий.

##### `ics(self) -> str`
Возвращает события в виде строки iCalendar.

##### `tokenize(self) -> list[tuple[Event, int]]`
Токенизирует события для выделения в текстовом поле.

##### `get_event_by_strindex(self, index: int) -> Event`
Возвращает событие по индексу человекочитаемой строки.

##### `pprint(self, person: people.Person=people.noone) -> str`
Печатает события в удобочитаемом формате.

##### `json(self) -> str`
Возвращает события в виде строки JSON.

##### `from_big_mess(cls, data: dict) -> Self`
Создает коллекцию событий из JSON, возвращаемого сервером.

##### `from_prepared_json(cls, data: dict) -> Self`
Получает события из подготовленного JSON. Используется для кэша.

##### `from_cache(cls, month: int, person_id: str) -> Self`
Получает события из кэша по месяцу и ID человека.

##### `to_cache(self, month: int, person_id: str) -> Self`
Сохраняет события в кэше.

##### `get_event_by_id(self, event_id: str) -> Event`
Возвращает событие с заданным идентификатором.

##### `get_events_by_date(self, date: date) -> Self`
Возвращает события для заданной даты.

##### `get_events_by_num(self, num) -> Self`
Возвращает события для заданного номера.

##### `get_events_between_times(self, start_time: time=..., end_time: time=...) -> Self`
Возвращает события между заданными начальным и конечным временем.

##### `get_events_between_dates(self, start_date: date=..., end_date: date=...) -> Self`
Возвращает события между заданными начальной и конечной датами.

##### `get_events_by_name(self, name: str) -> Self`
Возвращает события с заданным именем.

##### `get_events_by_teacher(self, teacher: str) -> Self`
Возвращает события с заданным преподавателем.

##### `get_events_by_room(self, room: str) -> Self`
Возвращает события с заданной аудиторией.

##### `get_events_by_status(self, status: str) -> Self`
Возвращает события с заданным статусом.

##### `overlap(self, other: Self) -> Self`
Возвращает пересечение между этой коллекцией событий и другой коллекцией событий.

##### `diff(self, other: Self) -> Self`
Возвращает разницу между этой коллекцией событий и другой коллекцией событий.

##### `human_diff(self) -> str`
Возвращает разницу в виде удобочитаемой строки.

##### `pprint_diff(self) -> str`
Печатает разницу в удобочитаемом формате, если есть.

##### `humanize(self, event_times: bool=True) -> Self`
Возвращает события в виде удобочитаемой строки.

##### `humanize_event(self, event_id: str) -> str`
Возвращает событие в виде удобочитаемой строки.

### Класс schedule.parsers.events.Event

Представляет собой одно событие.

#### Атрибуты
- `event_id` (str): Уникальный идентификатор события.
- `num` (str): Номер события.
- `date` (date): Дата события.
- `start_time` (time): Время начала события.
- `end_time` (time): Время окончания события.
- `duration` (timedelta): Продолжительность события.
- `room` (str): Аудитория события.
- `name` (str): Название события.
- `teacher` (str): Преподаватель события.
- `status` (str): Статус события.

#### Методы

##### `__init__(self, event_id: str, num: str, date: date, start_time: time, end_time: time, room: str, name: str, teacher: str, status: str) -> None`
Инициализирует объект события с заданными параметрами.

##### `__str__(self) -> str`
Возвращает строковое представление события.

##### `__eq__(self, other: Event) -> bool`
Проверяет, равны ли два события.

##### `__lt__(self, other: Event) -> bool`
Проверяет, является ли текущее событие меньшим по времени, чем другое событие.

##### `__gt__(self, other: Event) -> bool`
Проверяет, является ли текущее событие большим по времени, чем другое событие.

##### `__hash__(self) -> int`
Возвращает хэш-значение события.

##### `json(self) -> str`
Возвращает JSON-представление события.

##### `humanize(self, event_times: bool=True) -> str`
Возвращает событие в удобочитаемом формате.

##### `humanize_event_time(self) -> str`
Возвращает время события в удобочитаемом формате.

##### `humanize_duration(self) -> str`
Возвращает продолжительность события в удобочитаемом формате.

### Модуль: schedule.parsers.mess

#### Функция: `get_name(event_id, data)`

Извлекает имя события на основе его ID из предоставленных данных.

##### Параметры:
- `event_id` (int): ID события, для которого нужно извлечь имя.
- `data` (dict): Данные, содержащие реализации учебных курсов.

##### Возвращает:
- `str` или `None`: Имя события, если оно найдено, в противном случае `None`.

#### Функция: `get_teacher(event_id, data)`

Извлекает имя преподавателя для события на основе его ID из предоставленных данных.

##### Параметры:
- `event_id` (int): ID события, для которого нужно извлечь преподавателя.
- `data` (dict): Данные с сервера.

##### Возвращает:
- `str` или `None`: Имя преподавателя, если оно найдено, в противном случае `None`.

#### Функция: `get_room(event_id, data)`

Внутренняя функция. Работает с большим количеством данных.

#### Функция: `get_person_info(person_id, data)`

Извлекает информацию о человеке на основе его ID из предоставленных данных.

##### Параметры:
- `person_id` (int): ID человека, для которого нужно извлечь информацию.
- `data` (dict): Данные, содержащие информацию о людях (возвращенные сервером).

##### Возвращает:
- `tuple`: Тип человека и его информация.

#### Функция: `get_format_id(event_id, data)`

Извлекает формат события на основе его ID из предоставленных данных.

##### Параметры:
- `event_id` (int): ID события, для которого нужно извлечь формат.
- `data` (dict): Данные, содержащие форматы событий.

##### Возвращает:
- `str` или `None`: Формат события, если он найден, в противном случае `None`.

#### Функция: `get_type_id(event_id, data)`

Извлекает тип события на основе его ID из предоставленных данных.

##### Параметры:
- `event_id` (int): ID события, для которого нужно извлечь тип.
- `data` (dict): Данные, содержащие типы событий.

##### Возвращает:
- `str` или `None`: Тип события, если он найден, в противном случае `None`.

#### Функция: `get_type_and_format_name(event_id, data)`

Извлекает тип и формат события на основе его ID из предоставленных данных.

##### Параметры:
- `event_id` (int): ID события, для которого нужно извлечь тип и формат.
- `data` (dict): Данные, содержащие события.

##### Возвращает:
- `str`: Тип и формат события.

### Модуль: modeus

#### Функция: `modeus_parse_token(email: str, password: str) -> str`

Извлекает id_token с сервера modeus.

##### Параметры:
- `email` (str): электронная почта.
- `password` (str): пароль.

##### Возвращает:
- `str`: id_token.

##### Исключения:
- `RuntimeError`: если не удается разобрать 1-ю форму, 2-ю форму или id_token.

#### Функция: `modeus_auth(email: str, password: str) -> bool`

Проверяет, правильны ли учетные данные пользователя.

##### Параметры:
- `email` (str): электронная почта.
- `password` (str): пароль.

##### Возвращает:
- `bool`: `True`, если учетные данные верны, в противном случае `False`.

##### Исключения:
- `RuntimeError`: если не удается разобрать 1-ю форму или 2-ю форму.

#### Функция: `get_schedule(person_id: str, modeus_token: str, start_time: datetime, end_time: datetime) -> dict`

Получает расписание человека.

##### Параметры:
- `person_id` (str): идентификатор человека.
- `modeus_token` (str): токен modeus.
- `start_time` (datetime): начальное время.
- `end_time` (datetime): конечное время.

##### Возвращает:
- `dict`: огромный JSON с расписанием.

##### Исключения:
- `ValueError`: если `start_time >= end_time`.
- `RuntimeError`: если не удается найти вложенный ключ.

#### Функция: `get_schedule_async(person_id: str, modeus_token: str, start_time: datetime, end_time: datetime, on_finish, stop_event: asyncio.Event) -> None`

Получает расписание человека асинхронно.

##### Параметры:
- `person_id` (str): идентификатор человека.
- `modeus_token` (str): токен modeus.
- `start_time` (datetime): начальное время.
- `end_time` (datetime): конечное время.
- `on_finish` (функция): функция обратного вызова.
- `stop_event` (asyncio.Event): событие остановки.

##### Возвращает:
- `None`.

##### Исключения:
- `ValueError`: если `start_time >= end_time`.

#### Функция: `search_person(term: str, by_id: bool, modeus_token: str) -> dict`

Ищет человека в базе данных университета.

##### Параметры:
- `term` (str): поисковый запрос.
- `by_id` (bool): поиск по идентификатору или по полному имени.
- `modeus_token` (str): токен modeus.

##### Возвращает:
- `dict`: огромный JSON с результатами поиска.

##### Исключения:
- `RuntimeError`: если не удается найти вложенный ключ.

#### Функция: `who_goes(event_id: str, modeus_token: str) -> dict`

Получает участников события.

##### Параметры:
- `event_id` (str): идентификатор события.
- `modeus_token` (str): токен modeus.

##### Возвращает:
- `dict`: огромный JSON с участниками.

##### Исключения:
- `RuntimeError`: если не удается найти вложенный ключ.

#### Класс: `TimeCache`

Класс, кеширующий результаты функции на определенное время.

#### Функция: `is_connected()`

Функция, проверяющая наличие интернет-соединения. Кеширует результат на 5 секунд.



## 3.5 Разработка пользовательского интерфейса
Интерфейс программы разработан с помощью библиотеки WXPython. Он сделан максимально просто и понятно как для незрячих, так и для обычных пользователей.
### Главное окно
- В левой колонке находятся текстовые подсказки и дерево выбора даты. Встроенный виджет wx.adv.CalendarControl не поддерживает программы чтения с экрана, поэтому используется нативное дерево, в которое вручную прописаны даты.
- В правой колонке находится текстовое поле wx.TextCtrl, в котором отображается расписание на выбранную дату. В будущем планируется добавить форматирование текста, заголовки и другие элементы для удобства чтения.
- Ниже поля находится кнопка сохранения расписания в файл. При нажатии на неё программа откроет нативное окно сохранения файла.  
В верхней части окна есть классическая строка меню с подменю файл и помощь. В подменю файл есть пункты сохранить в файл, экспорт в календарь, чужое расписание, пересечение расписаний, очистить кеш и выход. В подменю помощь есть пункт о программе.
Снизу находится статусная строка, в которой выводятся подсказки и информация о текущем состоянии программы. При её изменении программа озвучивает новое состояние.
### Окна авторизации
При первом запуске программа открывает окно авторизации, в котором пользователь должен ввести логин и пароль. После ввода программа попросит ввести ФИО и сохранит его в кеше. Если нашлось более одного человека с таким ФИО, программа попросит выбрать нужного.
В окне авторизации есть поля ввода логина и пароля, кнопки отмены и подтверждения. При пустых полях окно не закрывается.
Окно ввода ФИО имеет поле ввода ФИО и кнопку подтверждения. При пустом поле ввода ФИО окно не закрывается. Если никого не нашлось, программа попросит ввести ФИО ещё раз.
Окно выбора человека имеет список wx.Choice с ФИО людей и кнопку подтверждения. Окно не появляется, если найден только один человек.
Все окна ввода представлены в guinput.py.
### Экспорт в календарь
При выборе пункта меню файл/экспорт в календарь программа откроет окно с QR-кодом, который нужно отсканировать, чтобы получить ссылку на скачивание календаря. Окно с QR-кодом закроется после того, как пользователь отсканирует его или нажмёт кнопку закрытия.
QR-код создаётся с помощью библиотеки qrcode и отображается в окне с помощью библиотеки Pillow и виджета wx.StaticBitmap.
# 4 Тестирование
## 4.1 Написание Unit-тестов
Юнит-тесты написаны с помощью библиотеки pytest.
В модуле tests.py есть 2 класса, TestRussianDate и TestEvents, которые проверяют работу соответствующих модулей в папке parsers.
## 4.2 Описание работы приложения
Главное окно программы состоит из двух колонок. В левой колонке производится выбор даты, в правой колонке отображается расписание на выбранную дату. В верхней части окна есть строка меню с дополнительными функциями. В нижней части окна есть статусная строка, в которой выводятся подсказки и информация о текущем состоянии программы.
При запуске программы открывается окно авторизации с полями ввода логина и пароля. Затем программа попросит ввести своё ФИО и сохранит его в кеше. Если нашлось более одного человека с таким ФИО, программа попросит выбрать нужного.
Если в главном окне сфокусироваться на определённом занятии и нажать меню файл/информация о паре, то откроется окно с подробной информацией о занятии и списком студентов, которые записаны на это занятие.
При выборе месяца в дереве дат, программа покажет расписание на этот месяц. При выборе дня в дереве дат, программа покажет расписание на этот день.
Пункт меню файл/сохранить в файл позволяет сохранить расписание в текстовый файл. При выборе программа откроет нативное окно сохранения файла.
Пункт меню файл/экспорт в календарь позволяет сохранить расписание в формате iCal. При выборе программа откроет окно с QR-кодом, который нужно отсканировать, чтобы получить ссылку на скачивание календаря. Окно с QR-кодом закроется после того, как пользователь отсканирует его.
Пункты меню файл/(чужое расписание \ пересечение расписаний) позволяют просмотреть расписание другого человека или пересечение расписаний с другим человеком. При выборе программа попросит ввести ФИО человека и покажет его расписание или пересечение расписаний.
пункт меню файл/очистить кеш позволяет удалить кеш программы. При выборе программа покажет сообщение о том, что кеш удалён, и закроется.
Пункт меню файл/выход позволяет выйти из программы. При выборе программа отправит сигнал потоку с логикой, чтобы он завершился, 1 секунду подождёт его завершения и закроется.
Пункт меню помощь/о программе открывает readme.txt в диалоговом окне.

# Заключение
Программа Schedule всё ещё в стадии бета-тестирования, но основной его функционал работает правильно. В будущем планируется доделать кроссплатформенность, переписать большинство модулей на Rust для прибавки производительности и снижения веса программы.

# Список использованных источников
- [python-dateutil](https://pypi.org/project/python-dateutil/)
- [pytz](https://pypi.org/project/pytz/)
- [wxPython](https://wxpython.org/)
- [pydotdict](https://pypi.org/project/pydotdict/)
- [httpx](https://pypi.org/project/httpx/)
- [packaging](https://pypi.org/project/packaging/)
- [icalendar](https://pypi.org/project/icalendar/)
- [qrcode](https://pypi.org/project/qrcode/)
- [pillow](https://pypi.org/project/pillow/)
- [markdown](https://pypi.org/project/markdown/)